

@{
    var student_email = Session["email"];

    if (student_email == null && Request.Url.ToString() != "http://localhost:50168/login.cshtml")
    {
        Response.Redirect("login.cshtml");
    }

    

}